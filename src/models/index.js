const BetModel = require('./bet');
const UserModel = require('./users');
const PropositionModel = require('./proposition-details');

module.exports = {
  BetModel,
  UserModel,
  PropositionModel,
};
